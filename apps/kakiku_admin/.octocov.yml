# generated by octocov init
coverage:
  paths:
    - coverage/lcov.info
  acceptable: 10%
  badge:
    path: coverage_badge.svg
push:
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
repository: andiasrafil/teka-teki-teku/apps/kakiku_admin
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
